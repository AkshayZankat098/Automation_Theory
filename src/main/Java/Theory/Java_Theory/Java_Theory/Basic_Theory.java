package Theory.Java_Theory.Java_Theory;

public class Basic_Theory {

/*

JDK > JRE > JVM
     JDK : (Complete java software) The first step of installation helps us to compile the code into machine language.
     JRE : Install to run java based applications
     JVM : Part of JRE & responsible for converting plain English code in 'byte code' and that understand by any machine.
           Also, the JVM is responsible for compiling (Check mistake in code line or not) our code.

Naming convention : PascalNamingConvention / camelNamingConvention / sank_naming_convention
Run code process : Source code >Compiler did by javac (Convert into byte code or machine understanding language : java source files into .class bytecode files) > JVM
String is Immutable & stringbuilder and stringbuffer is mutable

---> Stack and heap memory and Memory leaks:
---> Garbage collection concepts in java:
---> Exception Handling :

---> What is Data types (primitive & non-primitive) :
     Data Type : A classification that specifies what kind of value a variable can hold.
                 1. Primitive Data Types (Basic Types : Primitive data types are basic, built-in types that store simple values directly) :
                                         Integer (int), Float, Double, Character (char), Boolean (bool), Byte, Short, Long
                 2. Non-Primitive Data Types (Derived Types : Non-primitive data types are complex types that store collections of values or references) :
                                         Array, String , Structure (struct), Union, Class, Object, List, Tuple, Set, Dictionary (Map), File

---> Variable : Variable is a named memory location used to store data that can change during program execution.
                Local: Used within a function only.
                Global: Used throughout the program.
                Static: Keeps value between calls.
                Instance: Belongs to object.
                Class: Shared by all objects.

---> Casting: Converting a value from one data type to another.
              Implicit Casting (Type Conversion): Automatic conversion of a smaller data type to a larger data type by the compiler.
              Explicit Casting (Type Casting): Manual conversion of a data type into another type using programmer-defined syntax.

---> All Keywords :
                IMP:super IMP:this IMP:static  abstract  assert  break  class  const  continue  default  extends  final  finally  implements  interface  private  protected
                public return switch


 ---> What is static and non - static methods ::
 Static methods: Can be called without creating an object of the class, Belong to the class. (Syntax: ClassName.staticMethodName();)
 As we do not required creating object So we can manage memory using static method

 Non-static methods: Require an object of the class to be called, Belong to instances (objects) of the class. (ClassName obj = new ClassName(); obj.nonStaticMethodName();)
 Rule ::
    "static" methods belongs to the class, not to the object.
    "static" method can be accessed directly by class name and doesn’t need any object.
    "static" method can access only static data. It cannot access non-static data (instance data).
    "static" method can call only other static methods and cannot call a non-static method.
    "static" method cannot refer to "this" or "super" keyword in any way.

---> Access modifiers:
                        public
                        private
                        protected
                        default
 ---> Wrapper Classes :

 --> What is parameters and arguments:
                                     Parameters : String name in greet(String name)
                                     Arguments : "Akshay" in greet("Akshay")








*/




}
